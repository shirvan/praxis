{{- range $name, $config := .Values.drivers }}
{{- if $config.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "praxis.fullname" $ }}-{{ $name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "praxis.labels" $ | nindent 4 }}
    {{- include "praxis.selectorLabels" (dict "root" $ "component" $name) | nindent 4 }}
spec:
  selector:
    {{- include "praxis.selectorLabels" (dict "root" $ "component" $name) | nindent 4 }}
  ports:
    - name: restate
      port: 9080
      targetPort: restate
{{- end }}
{{- end }}
